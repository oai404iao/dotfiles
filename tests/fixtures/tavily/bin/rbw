#!/bin/sh
case "$*" in
    *"pi tavily api key"*) password="tvly-test-key" ;;
    *) exit 1 ;;
esac

printf '{"id":"test","folder":null,"name":"test","data":{"username":"","password":"%s","totp":null,"uris":[]},"fields":[],"notes":null,"history":[]}\n' "$password"
