[project]
name = "waybar-longshot"
version = "1.0.0"
description = "Python dependencies for the Waybar long screenshot helper"
requires-python = ">=3.12,<3.13"
dependencies = [
    "numpy>=2.0,<3",
    "opencv-python-headless>=4.12,<5",
]

[tool.uv]
package = false
