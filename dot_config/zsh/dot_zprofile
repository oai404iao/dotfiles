# niri-session starts the selected shell as a login shell and imports its
# environment into the user service manager.

profile_file="$XDG_CONFIG_HOME/shell/profile.sh"
[[ -r "$profile_file" ]] && source "$profile_file"
unset profile_file
